# =============================================================================
# requirements.txt
# Project: Roundtable App
# Stack:   Node / React 19 / Vite 8 / TypeScript 6
# =============================================================================
#
# REFERENCE / INVENTORY FILE ONLY.
# This file does NOT replace package.json or package-lock.json.
# npm uses package.json + package-lock.json for all installs.
#
# To install:   npm ci          (clean install from lockfile)
#               npm install     (if no lockfile yet)
#
# Exact versions are resolved from the working roundtable-app/node_modules
# install, supplemented by root/node_modules for gsap and lenis.
#
# Node.js requirement: >= 18.x (LTS recommended: 20.x or 22.x)
# =============================================================================


# =============================================================================
# RUNTIME DEPENDENCIES
# (shipped to the browser / required at application runtime)
# =============================================================================

# React UI framework
react==19.3.0
react-dom==19.3.0

# Client-side routing
react-router-dom==7.18.3

# Animation engine
# gsap includes ScrollTrigger as a built-in plugin (gsap/ScrollTrigger).
# ScrollTrigger is NOT a separate npm package.
gsap==3.15.0

# Smooth-scroll library
lenis==1.3.26


# =============================================================================
# DEVELOPMENT DEPENDENCIES
# (build-time only — not shipped to browser)
# =============================================================================

# ── Build tool ──────────────────────────────────────────────────────────────
vite==8.3.0
@vitejs/plugin-react==6.1.1

# ── TypeScript compiler ─────────────────────────────────────────────────────
typescript==6.0.3

# ── TypeScript type definitions ─────────────────────────────────────────────
@types/react==19.3.0
@types/react-dom==19.3.0
@types/node==24.13.4

# ── ESLint (linter) ─────────────────────────────────────────────────────────
eslint==10.10.0
@eslint/js==10.0.1
globals==17.12.0
typescript-eslint==8.70.0
eslint-plugin-react-hooks==7.1.1
eslint-plugin-react-refresh==0.5.6


# =============================================================================
# EXPLICITLY EXCLUDED
# =============================================================================

# @gsap/react
#   Was present in the old root node_modules but is NOT imported anywhere
#   in the application source code. No file in src/ uses useGSAP or any
#   other export from @gsap/react. Deliberately omitted.
